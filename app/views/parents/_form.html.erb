<%= form_for [ book, @parent ] do |f| %>
  <%= f.hidden_field :book_id, value: book.id %>
  <%= f.hidden_field :deadline, value: Time.now() + 1.week %>
  <%= f.submit "取引に出す", data: { confirm: "誰かがこの本にほしいね！を押すと取引を取り消すことができなくなりますが、よろしいですか？"}  %>
<% end %>
